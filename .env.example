# Puerto del servidor
PORT=3000

# Clave de autenticación para la API (opcional, si se deja vacío los endpoints son públicos)
API_KEY=

# Directorio donde se guardará la sesión autenticada de Baileys
SESSION_DIR=./sessions

# Nivel de log (fatal, error, warn, info, debug, trace)
LOG_LEVEL=info
